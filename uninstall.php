<?php
// Prevent direct access
if (!defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}
// Clean up options if needed
// delete_option('vendor_sync_settings');
// delete_option('vendor_batch_*');
// Or keep it empty for now